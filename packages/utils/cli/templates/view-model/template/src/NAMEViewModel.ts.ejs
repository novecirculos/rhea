import { makeAutoObservable } from 'mobx';

import { <%= props.pascalCaseName %>Model } from '@novecirculos/<%= props.kebabCaseName %>-model';

export class <%= props.pascalCaseName %>ViewModel {
  private _model: <%= props.pascalCaseName %>Model;

  constructor(model: <%= props.pascalCaseName %>Model) {
    this._model = model;
    makeAutoObservable(this);
  }

  get count() {
    return this._model.count.toString();
  }

  onPressButton = () => {
    this._model.increaseCount();
  };
}
