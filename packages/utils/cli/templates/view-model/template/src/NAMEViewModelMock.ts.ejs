import { <%= props.pascalCaseName %>ViewModel } from './<%= props.pascalCaseName %>ViewModel';
import { action } from '@storybook/addon-actions';

export const <%= props.pascalCaseName %>ViewModelMock: Pick<<%= props.pascalCaseName %>ViewModel, keyof <%= props.pascalCaseName %>ViewModel> = {
  count: '5',
  /**
  * Actions here show up on storybook's actions panel when called.
  * It automatically logs all parameters passed to the action.
  */
 onPressButton: action('on-press-button'),
};
