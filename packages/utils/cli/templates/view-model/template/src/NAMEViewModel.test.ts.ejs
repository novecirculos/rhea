import { describe, expect, test } from '@jest/globals';

describe('<%= props.pascalCaseName %> ViewModel', () => {
  test('Exposes count from model', () => {
    expect(true).toBe(false);
  });
  test('Count updates when model changes', () => {
    expect(true).toBe(false);
  });
});
