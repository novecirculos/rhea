import { makeAutoObservable } from 'mobx';


/**
 * Model containing all dependencies and state for the <%= props.pascalCaseName %> feature.
 */
export class <%= props.pascalCaseName %>Model {
  constructor({ count = 0 }: Pick<<%= props.pascalCaseName %>Model, 'count'>) {
    this.count = count;
    makeAutoObservable(this);
  }

  /**
   * The current count. Initial value received as a dependency.
   */
  count = 0;

  /**
  * Implementation of the `increaseCount` action. This is meant to be called by
  * the View Model.
  */
 increaseCount = () => {
   this.count += 1;
 };
}
