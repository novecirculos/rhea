import { useContext } from 'react';
import { <%= props.pascalCaseName %>Context } from '../contexts/<%= props.pascalCaseName %>Context';
import { <%= props.pascalCaseName %>Store } from '../stores/<%= props.pascalCaseName %>Store';

export const use<%= props.pascalCaseName %>: () => <%= props.pascalCaseName %>Store = () => {
  const context = useContext(<%= props.pascalCaseName %>Context);

  if (!context) {
    throw new Error(
      'use<%= props.pascalCaseName %> must be used within an <%= props.pascalCaseName %>Provider'
    );
  }

  return context;
};
