import { createContext } from 'react';
import { <%= props.pascalCaseName %>Store } from '../stores/<%= props.pascalCaseName %>Store';

export const <%= props.pascalCaseName %>Context = createContext<<%= props.pascalCaseName %>Store>(
  {} as <%= props.pascalCaseName %>Store
);
