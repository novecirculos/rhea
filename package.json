{
  "name": "@novecirculos/rhea",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/*/*",
    "apps/*"
  ],
  "devDependencies": {
    "@graphql-codegen/cli": "^3.3.0",
    "@graphql-codegen/import-types-preset": "^2.2.6",
    "@graphql-codegen/typescript": "^3.0.3",
    "@graphql-codegen/typescript-operations": "^3.0.3",
    "@graphql-codegen/typescript-react-apollo": "^3.3.7",
    "@novecirculos/cli": "*",
    "@types/react": "^18.0.17",
    "@types/react-native": "^0.69.5",
    "dotenv": "^16.0.3",
    "eslint": "^8.21.0",
    "graphql-codegen-apollo-next-ssr": "^1.7.4",
    "prettier": "^2.7.1",
    "prettier-plugin-tailwindcss": "^0.1.13",
    "turbo": "^1.4.2",
    "typescript": "^4.7.4"
  },
  "scripts": {
    "prestart": "dotenv -e .env lerna run --scope=@novecirculos/* dev",
    "dev": "rhea dev",
    "build": "turbo build",
    "nuke": "yarn nuke:packages && yarn nuke:apps && yarn nuke:features && yarn nuke:lockfile",
    "nuke:packages": "rm -r node_modules; for d in packages/*/*/node_modules; do echo $d; rm -r $d; done",
    "nuke:apps": "rm -r node_modules; for d in apps/*/node_modules; do echo $d; rm -r $d; done",
    "nuke:lockfile": "for d in ./*.lock; do echo $d; rm -rf $d; done",
    "nuke:dist": "rm -r dist; for d in packages/*/dist; do echo $d; rm -r $d; done",
    "reset": "yarn nuke && yarn install",
    "codegen": "graphql-codegen"
  },
  "dependencies": {
    "vercel": "^29.3.0"
  }
}
