query GetArticleByTitle($title: String) {
  article(where: { title: $title }) {
    id
    slug
    category
    title
    content {
      json
      markdown
      html
    }
    image {
      url
    }
  }
}
