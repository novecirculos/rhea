query GetArticleByTitle($title: String) {
  article(where: { title: $title }) {
    id
    slug
    category
    title
    universeDate
    content {
      json
      markdown
      html
    }
    image {
      url
    }
  }
}
