
import { z } from "zod";

export const <%= props.pascalCaseName %>Schema = z.object({
  gender: z.enum(["Male", "Female"]),
  race: z.enum(["Human", "Elf", "Dwarf", "Halfling", "HalfElf", "Gnome"]),
});
