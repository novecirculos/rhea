#!/usr/bin/env bun

/* tslint:disable */

// Require the module and store it in a variable
var baseSourceDir = __dirname
var sourceDir = `${baseSourceDir}/cli.ts`

// Require the module and store it in a variable
var cliModule = require(sourceDir)

cliModule.main(process.argv.slice(2))
